#!/usr/bin/env bash
# LinuxEnv Helper 仓库内启动器（无需安装即可运行）
# 用法: sudo bash bin/leh   或   ./bin/leh
SELF="$(readlink -f "${BASH_SOURCE[0]}")"
REPO_DIR="$(dirname "$(dirname "$SELF")")"
cd "$REPO_DIR" && exec sudo bash main.sh "$@"
